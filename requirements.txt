# 活码管理系统依赖
# 使用时：NO_PROXY="*" python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# Web 框架
fastapi>=0.115
uvicorn[standard]

# 数据库
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20           # SQLite 异步驱动（本地开发）
aiomysql>=0.3             # MySQL 异步驱动（腾讯云生产环境）
alembic>=1.13
pydantic>=2.0
pydantic-settings>=2.0

# 后台管理
sqladmin>=0.19
itsdangerous>=2.2

# 模板 & 前端
jinja2>=3.1
python-multipart          # 文件上传

# 二维码 & 图片
qrcode[pil]>=8.0
pillow>=10.0

# 导出
openpyxl>=3.1

# 测试
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27               # 异步测试客户端